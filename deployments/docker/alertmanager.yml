# Alertmanager configuration has been consolidated.
# See: deployments/monitoring/alertmanager/alertmanager.yml
#
# To use with Docker Compose, mount the canonical file:
#   volumes:
#     - ../monitoring/alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml
