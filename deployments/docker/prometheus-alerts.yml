# Prometheus alerting rules have been consolidated.
# See: deployments/monitoring/prometheus/rules/apicerberus-alerts.yml
#
# To use with Docker Compose, mount the canonical file:
#   volumes:
#     - ../monitoring/prometheus/rules/apicerberus-alerts.yml:/etc/prometheus/rules/apicerberus-alerts.yml
